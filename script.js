// Define an array with at least 5 elements, including a string, a number, and a boolean
let yourArray = ['hello', 42, true, 'world', 3.14];

// Log the array to the console
console.log(yourArray);

// Log the length of the array
console.log(yourArray.length);

